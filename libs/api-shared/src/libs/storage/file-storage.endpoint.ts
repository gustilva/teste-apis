export enum FileStorageEndpointPaths {
    CallBack = '/storage/callback',
    Buckets = '/storage/buckets',
    Upload = '/storage/upload',
    DownloadBuffer = '/storage/download-buffer',
    DownloadFile = '/storage/download-file',
    SignedUrl = '/storage/signed-url',
    Delete = '/storage/delete-file',
}
